import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';
import sidebars from './sidebars.json';

// Keep the legacy navigation ordering while the content runs on the Docs2 framework.
export default sidebars as SidebarsConfig;
